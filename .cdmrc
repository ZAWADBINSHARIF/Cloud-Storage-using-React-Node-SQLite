# terminals
terminals:
    [
        'cd Client && yarn && yarn start',
        'cd Server && yarn && yarn dev',
        'echo "We''re ready"',
    ]
# other config
tabs: ['README.md']
run-button: clear && node $$file
browser-link: http://localhost:1337
